{% props icon = null, variant = 'primary' %}

<span {{ attributes.defaults({ class: 'badge' }) }}>
	{% if icon %}
		<twig:Icon>{{ icon }}</twig:Icon>
	{% endif %}

    {% block content %}{% endblock %}
</span>

